spring:
  datasource:
    url: jdbc:mysql://localhost:3307/cukcuk-database
    username: root
    password: root
  #    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    org:
      springframework: debug

api:
  prefix: api/v1
